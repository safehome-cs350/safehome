services:
  backend:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile.backend
    volumes:
      - ..:/workspace
    working_dir: /workspace
    user: vscode
    command: sleep infinity
